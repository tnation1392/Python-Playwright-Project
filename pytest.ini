[pytest]
base_url = https://playwright.dev
addopts = --tracing=retain-on-failure --screenshot=only-on-failure --full-page-screenshot
testpaths = tests
markers =
    smoke: critical path tests
    regression: broader coverage tests